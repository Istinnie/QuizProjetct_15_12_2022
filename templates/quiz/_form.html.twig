{{ form_start(form) }}
    {# {{ form_widget(form) }} #}

        {{ form_errors(form) }}
            <div class="mt-2">
                {{ form_row(form.NumeroQuiz, {
                label: 'N° de quiz'
            }) }}
            </div>
            
            <div class="mt-3">
                {{ form_row(form.pro, {
                label: 'Référence du professionel'
            }) }}
                
            </div>

    <button class="btn btn-outline-info">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
