{% extends 'base.html.twig' %}

{% block title %}CategorieUtilisateur{% endblock %}

{% block body %}
    <h1>CategorieUtilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorie_utilisateur.id }}</td>
            </tr>
            <tr>
                <th>LibelleCategorie</th>
                <td>{{ categorie_utilisateur.LibelleCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorie_utilisateur_index') }}">back to list</a>

    <a href="{{ path('app_categorie_utilisateur_edit', {'id': categorie_utilisateur.id}) }}">edit</a>

    {{ include('categorie_utilisateur/_delete_form.html.twig') }}
{% endblock %}
