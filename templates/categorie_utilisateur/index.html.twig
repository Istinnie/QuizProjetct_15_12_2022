{% extends 'base.html.twig' %}

{% block title %}CategorieUtilisateur index{% endblock %}

{% block body %}
    <h1>CategorieUtilisateur index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>LibelleCategorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_utilisateur in categorie_utilisateurs %}
            <tr>
                <td>{{ categorie_utilisateur.id }}</td>
                <td>{{ categorie_utilisateur.LibelleCategorie }}</td>
                <td>
                    <a href="{{ path('app_categorie_utilisateur_show', {'id': categorie_utilisateur.id}) }}">show</a>
                    <a href="{{ path('app_categorie_utilisateur_edit', {'id': categorie_utilisateur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_utilisateur_new') }}">Create new</a>
{% endblock %}
