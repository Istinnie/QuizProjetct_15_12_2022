{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
    <nav class="m-2 ">
        <ul class="nav nav-pills nav-fill ">
            <li class="nav-item m-2"><a class="nav-link" href="{{ path('app_quiz_index') }}">Quiz</a></li>
            <li class="nav-item  m-2"><a class="nav-link" href="{{ path('app_question_index') }}">Question</a></li>
            <li class="nav-item  m-2"><a class="nav-link" href="{{ path('app_reponse_index') }}">Réponse</a></li>
            <li class="nav-item  m-2"><a class="nav-link" href="{{ path('app_users') }}">Utilisateurs</a></li>
            <li class="nav-item  m-2"><a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a></li>
        </ul>
    </nav>
    <div class="m-5">
    
        <h1 class="text-center">Liste des utilisateurs</h1>
        <table class="table-quiz ml-5 mt-5">
        <thead>
            {# <th>ID</th> #}
            <th>Nom d'utilisateur</th>
            <th>Rôles utilisateur</th>
            <th>Actions</th>
        </thead>
        <tbody>
            {# On boucle sur les utilisateurs #}
            {% for user in users %}
                <tr>
                    {# <td>{{ user.id }}</td> #}
                    <td>{{ user.username }}</td>
                    <td>
                        {# On boucle sur les rôles #}
                        {% for role in user.roles %}
                            {% if role == "ROLE_USER" %}
                                {# Utilisateur #}
                            {% endif %}
                            {% if role == "ROLE_PRO" %}
                                Professionnel
                            {% endif %}
                            {% if role == "ROLE_ADMIN" %}
                                Administrateur
                            {% endif %}
                        {% endfor %}
                    </td>
                    <td> <a href="{{ path('admin_modifier_utilisateur', {'id': user.id}) }}" class="btn btn-outline-secondary ml-2">
                            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-pencil-fill" viewBox="0 0 16 16">
                            <path d="M12.854.146a.5.5 0 0 0-.707 0L10.5 1.793 14.207 5.5l1.647-1.646a.5.5 0 0 0 0-.708l-3-3zm.646 6.061L9.793 2.5 3.293 9H3.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.5h.5a.5.5 0 0 1 .5.5v.207l6.5-6.5zm-7.468 7.468A.5.5 0 0 1 6 13.5V13h-.5a.5.5 0 0 1-.5-.5V12h-.5a.5.5 0 0 1-.5-.5V11h-.5a.5.5 0 0 1-.5-.5V10h-.5a.499.499 0 0 1-.175-.032l-.179.178a.5.5 0 0 0-.11.168l-2 5a.5.5 0 0 0 .65.65l5-2a.5.5 0 0 0 .168-.11l.178-.178z"/>
                            </svg>
                            Modifier</a></td>
                </tr>
                
                {% else %}
                    <p>Aucun utilisateur inscrit</p>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
