{% extends 'base.html.twig' %}

{% block title %}Quizzer{% endblock %}

{% block body %}

    <section >
        <div class="content">
            <div class="header-left">
                <img src="{{ asset('img/QuizLogo.jpg') }}" alt="Logo">
            </div>
            <div class="header-right">
                <div>
                    <h1 class="mt-5">Bienvenue sur Quizzer</h1>
                </div>
                <ul class="nav mt-5">
                {% if not app.user %}
                    <li class="nav-item  "><a class="btn btn-outline-primary mt-4" href="{{ path('app_register') }}">S'enregistrer</a></li>
                    <li class="nav-item  "><a class="btn btn-outline-primary mt-4 ml-4" href="{{ path('app_login') }}">Se connecter</a></li>
                {% else %}
                    <li class="nav-item  "><a class="btn btn-outline-primary mt-4" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                {% endif %}
                </ul>
            </div>
            <div>
                
            </div>

        </div>
        

    </section>
        

{% endblock %}
