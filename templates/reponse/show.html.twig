{% extends 'base.html.twig' %}

{% block title %}Reponse{% endblock %}

{% block body %}
    <nav class=" m-2 ">
    <ul class="nav nav-pills nav-fill ">
        <li class="nav-item m-2"><a class="nav-link" href="{{ path('app_quiz_index') }}">Quiz</a></li>
        <li class="nav-item  m-2"><a class="nav-link" href="{{ path('app_question_index') }}">Question</a></li>
        <li class="nav-item  m-2"><a class="nav-link" href="{{ path('app_reponse_index') }}">Réponse</a></li>
        <li class="nav-item  m-2"><a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a></li>
    </ul>
    </nav>
    <div class="m-5">
        <h1 class="align-middle">Réponse</h1>

        <table class=" table mt-5 mb-5">
            <tbody>
                <tr>
                    <th>Numéro de question :</th>
                    <td>{{ reponse.question}}</td>
                </tr>
                <tr>
                    <th>Libellé de la question :</th>
                    <td>{{ reponse.question.LibelleQuestion}}</td>
                </tr>
                
                {# <tr>
                    <th>Id</th>
                    <td>{{ reponse.id }}</td>
                </tr> #}
                <tr>
                    <th>Libellé de la reponse :</th>
                    <td>{{ reponse.LibelleReponse }}</td>
                </tr>
            </tbody>
        </table>
    <a href="{{ path('app_reponse_edit', {'id': reponse.id}) }}" class="btn btn-outline-secondary">Editer</a>
    <a href="{{ path('app_reponse_index') }}" class="btn btn-outline-primary">Retourner à la liste</a>
    <div>{{ include('reponse/_delete_form.html.twig') }}</div>
        

    </div>
{% endblock %}
